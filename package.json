{
  "name": "jsonresume-vite-boilerplate",
  "version": "0.1.0",
  "description": "Resume Boilerplate built with JSONResume, Vite and Tailwind CSS",
  "author": "Pat Webster",
  "private": true,
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm build && gh-pages -d dist/",
    "export": "npx resume export resume --format pdf --theme ."
  },
  "dependencies": {
    "autoprefixer": "^10.4.7",
    "postcss": "^8.4.14",
    "tailwindcss": "^3.1.5",
    "vite": "^2.9.14",
    "vite-plugin-handlebars": "^1.6.0",
    "vite-plugin-singlefile": "^0.10.0"
  },
  "devDependencies": {
    "gh-pages": "^4.0.0",
    "resume-cli": "^3.0.7",
    "resumed": "^1.0.3"
  }
}
